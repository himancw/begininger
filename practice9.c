#include<stdio.h>
main()
{
	int num=0;
	for (printf("enter a number:"); num != 99;)  //The first expression not necessarily be assigning initial value for variate.
	(void)scanf("%d", &num);
	printf("you are right!");
}
